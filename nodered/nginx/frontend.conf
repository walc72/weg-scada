server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Resolver Docker DNS + variable para que nginx re-resuelva en cada request
    resolver 127.0.0.11 valid=10s ipv6=off;
    set $api_upstream http://weg-api:3200;
    set $mqtt_upstream http://mosquitto:9001;

    # Proxy /api/* to weg-api (no trailing slash = path preserved as-is)
    location /api/ {
        proxy_pass $api_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # MQTT sobre WebSocket para el frontend (broker interno, solo lectura por ACL)
    location /mqtt {
        proxy_pass $mqtt_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # SPA fallback — all unmatched routes → index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache hashed assets forever, never cache index.html
    location ~* \.(?:js|css|woff2?|png|jpg|svg|ico)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    location = /index.html {
        add_header Cache-Control "no-cache";
    }
}
